<h2>People</h2>

<ul class="people">
  {{#each people as |person|}}
    <li {{action (action "gotoPersonGoals" person)}}>
      <span class="name">{{person.firstName}} {{person.lastName}}</span>
      <span class="score">{{person.score}}</span>
    </li>
  {{/each}}
</ul>

Total people: {{people.length}}

{{outlet}}